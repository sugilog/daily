<%= form_for @user,
             url: (availables == [:password] ? {action: :update_password, id: @user} : user_path(@user)),
             html: { class: 'form-horizontal' } do |f| %>
  <% if @user.errors.any? %>
    <div class='alert alert-error'>
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% if availables.include?(:name) %>
  <div class="control-group">
    <%= f.label :name, class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, class: 'text_field' %>
    </div>
  </div>
<% end %>
<% if availables.include?(:email) %>
  <div class="control-group">
    <%= f.label :email, class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :email, class: 'text_field' %>
    </div>
  </div>
<% end %>
<% if availables.include?(:password) %>
  <div class="control-group">
    <%= f.label :password, class: 'control-label' %>
    <div class="controls">
      <%= f.password_field :password, class: 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :password_confirmation, class: 'control-label' %>
    <div class="controls">
      <%= f.password_field :password_confirmation, class: 'text_field' %>
    </div>
  </div>
<% end %>

  <div class="form-actions">
    <%= f.submit nil, class: 'btn btn-primary' %>
    <%= link_to t('.cancel', default: t("helpers.links.cancel")), {controller: :sessions, action: :new}, {class: 'btn'} %>
  </div>
<% end %>
